/**
 * Anchor placed inside the currently playing episode card.
 * Previously auto-scrolled to the current episode on mount — removed
 * because the scroll was disruptive.  The component is kept so the
 * "Now Playing" badge logic in TVSeasons.tsx stays unchanged.
 */
const EpisodeScrollAnchor = () => {
  return null;
};

export default EpisodeScrollAnchor;
